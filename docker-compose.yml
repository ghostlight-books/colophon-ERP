version: "3.9"
services:
  app:
    image: node:20-alpine
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: sh -c "npm install && npm run dev"
    ports:
      - "4000:4000"
      - "5173:5173"
    environment:
      - NODE_ENV=development
